{
  "name": "barq-e-insaf-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix Frontend",
    "start": "npm start --prefix Backend",
    "dev": "npm run dev --prefix Backend"
  }
}
